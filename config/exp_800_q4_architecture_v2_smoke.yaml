# Fast integration gate for Architecture V2. Not a performance experiment.
extends: exp_800_q4_architecture_v2.yaml

scenario:
  T: 20

marl:
  rollout_steps: 64
  minibatch_size: 32
  num_envs: 1
  num_episodes: 1
  eval_interval: 100
  eval_episodes: 1
  early_stop: false
  checkpoint_confirmation_enabled: false
  training_seed_replay_enabled: false
  target_allocation_aux_epochs: 1

