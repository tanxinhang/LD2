# D1.10-C A/B variant (advice 014 §4/§5, 2026-08-17): adds the coupling-aware
# structure repair to the D1.10 independent-sampling certification config.
# Only frames where t_fix < d_req <= t_relax (structural power congestion, e.g.
# blind100 seed 615) trigger the sparse 1-swap owner/support search, accepted
# strictly lexicographically (QoS feasibility > floor violation > t_min) and
# only with a substantial improvement (churn guard), else No-op -- so
# already-good frames are untouched (no-regression by construction).
extends: exp_800_k8q8_analytical_l0l1_movement_lex_candidates_blind_lookahead40_indep.yaml

marl:
