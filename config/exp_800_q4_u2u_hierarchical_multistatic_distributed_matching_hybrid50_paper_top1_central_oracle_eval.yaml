# Gate 2A positive control: explicitly reconstruct and centrally fuse every
# selected airborne receiver's evidence. This must match legacy_global.
extends: exp_800_q4_u2u_hierarchical_multistatic_distributed_matching_hybrid50_paper_top1_eval.yaml

marl:
  detection_fusion_mode: central_oracle
