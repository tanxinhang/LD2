# Capacity-aware compromise: retain the learned top-2 contract and open at
# most one extra target token for a locally capable underloaded target.
extends: exp_800_q4_u2u_hierarchical_multistatic_distributed_matching_hybrid50_semantic_decoder_eval.yaml

marl:
  comm_semantic_extra_token_enabled: true
  comm_semantic_extra_token_threshold: 0.10
