# Fixed communication/sensing split ablation. Each active UAV assigns 0.25 W
# to communication and 0.75 W to sensing under the same exact 1 W budget.
# Target-wise sensing allocation remains learned, isolating the split head.
extends: exp_800_q4_u2u_hierarchical_multistatic_distributed_matching_hybrid50_eval.yaml

marl:
  comm_power_fraction_min: 0.25
  comm_power_fraction_max: 0.25
