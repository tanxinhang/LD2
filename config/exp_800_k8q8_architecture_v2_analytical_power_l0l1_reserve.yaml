# D0.93 L0+L1(reserve-first): comm-slack recovery + reserve-first power LP
# (per-target floor 0.60 enforced before max-min).
extends: exp_800_k8q8_architecture_v2_analytical_power_l0l1.yaml

marl:
  analytical_sensing_power_reserve_pd: 0.6
