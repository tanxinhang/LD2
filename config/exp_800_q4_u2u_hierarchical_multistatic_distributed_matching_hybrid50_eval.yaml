# Bid-inertia candidate: mix stable projected commitments with live intrinsic
# bids to permit correction without discarding assignment persistence.
extends: exp_800_q4_u2u_hierarchical_multistatic_distributed_matching_eval.yaml

marl:
  # Historical paper family: retain the old environment-level global evidence
  # sum only for exact regression. New deployable experiments must override
  # this with local_only or u2u_distributed.
  detection_fusion_mode: legacy_global
  movement_team_matching_intrinsic_bid_mix: 0.5
