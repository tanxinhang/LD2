# Dense-broadcast communication baseline. Every sender transmits all four
# target tokens; token dimension, quantization, power and policy are unchanged.
extends: exp_800_q4_u2u_hierarchical_multistatic_distributed_matching_hybrid50_eval.yaml

marl:
  sparse_claim_share_topk: 4
