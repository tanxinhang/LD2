# Persistent-intent prototype: retain each physically transmitted sparse
# commitment for a slow-layer interval and use a bounded two-frame union while
# handing over from the old target set to the new one.
extends: exp_800_q4_architecture_v2_maxmin_token_commitment_gate.yaml

marl:
  distributed_target_commitment_min_hold_frames: 5
  distributed_target_commitment_handover_frames: 2
