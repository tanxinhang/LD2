# Conservative structural probe: retain half of the actor's local movement
# commitment and blend in half of the token-reconstructed one-to-one team graph.
extends: exp_800_q4_u2u_hierarchical_multistatic_distributed_matching_eval.yaml

marl:
  movement_team_matching_blend: 0.5
