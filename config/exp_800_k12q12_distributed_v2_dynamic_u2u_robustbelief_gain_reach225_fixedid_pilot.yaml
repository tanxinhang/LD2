# Far-stage coverage diagnostic: retain the reachability-first two-stage
# controller and d_safe shield, but replace cache-dependent movement matching
# by the shared target-ID bijection.  This needs no ACK, owner certificate, or
# optimizer broadcast and guarantees 100% target coverage when K=Q.
extends: exp_800_k12q12_distributed_v2_dynamic_u2u_robustbelief_gain_schedule2_pilot.yaml

marl:
  distributed_gain_scheduled_far_assignment_mode: fixed_id
