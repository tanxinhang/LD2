# Matched no-U2U baseline. The actor, target geometry, 1 W RF budget and
# multistatic sensing stack are unchanged; every learned broadcast is forced to
# rate zero during evaluation. The released communication power returns to the
# sensing budget through the same hard power-balance equation.
extends: exp_800_q4_u2u_hierarchical_multistatic_distributed_matching_hybrid50_eval.yaml

marl:
  comm_eval_force_silence: true
