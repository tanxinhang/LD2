# Matched fixed-cardinality comparison: the only change from the paper full
# model is one target token per sender instead of two.
extends: exp_800_q4_u2u_hierarchical_multistatic_distributed_matching_hybrid50_eval.yaml

marl:
  sparse_claim_share_topk: 1
