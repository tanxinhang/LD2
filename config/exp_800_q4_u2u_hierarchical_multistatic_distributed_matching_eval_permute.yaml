# Sender/content correspondence ablation for the consensus-bid token
# architecture. Communication resource use is unchanged.
extends: exp_800_q4_u2u_hierarchical_multistatic_distributed_matching_eval.yaml

marl:
  comm_eval_message_ablation: permute
