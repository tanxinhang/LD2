# Frozen non-saturated control profile selected on calibration seeds that are
# excluded from confirmation.  The lower sensing cap removes the K=2/Q=2
# smoke's near-unit-P_D ceiling while retaining topology, RF accounting, CVaR
# floor and execution solver semantics.
extends: exp_temporal_feasible_structure_smoke.yaml

uav:
  P_sense: 0.15
  P_sense_max: 0.15

marl:
  num_episodes: 3
