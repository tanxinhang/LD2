# Deployable graph fallback diagnostic.  Distributed commitments remain the
# ranking prior, while physically valid low-priority edges are retained with a
# confidence-adaptive floor instead of being deleted by hard Top-2 masking.
extends: exp_800_q4_architecture_v2_maxmin_filtered_gate.yaml

marl:
  distributed_target_commitment_mode: soft
  distributed_target_commitment_soft_floor: 0.25
  distributed_target_commitment_uncertainty_relief: 1.0
