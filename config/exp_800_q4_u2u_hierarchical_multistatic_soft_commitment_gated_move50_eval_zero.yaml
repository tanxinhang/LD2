# Semantic-content ablation: preserve the learned communication schedule,
# bitrate, link delivery, latency, power allocation, and token-presence masks,
# while replacing every delivered target token with zero.
extends: exp_800_q4_u2u_hierarchical_multistatic_soft_commitment_gated_move50_eval.yaml

marl:
  comm_eval_message_ablation: zero
