# Ablation of the direct received-token residual on target-wise sensing power.
# Movement consensus and the capacity-two target allocation remain active.
extends: exp_800_q4_u2u_hierarchical_multistatic_distributed_matching_hybrid50_eval.yaml

marl:
  comm_aided_sensing_enabled: false
  comm_aided_sensing_blend: 0.0
