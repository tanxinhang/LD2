# Short mechanism test for HTM-GMAPPO.  This is not a publication run: the
# coupling schedules are compressed so failures/successes become visible in a
# few thousand transitions before committing to the 150k-frame schedule.
extends: exp_800_q4_u2u_hierarchical_multistatic.yaml

marl:
  rollout_steps: 512
  minibatch_size: 128
  num_envs: 4
  early_stop: false
  capacity_matching_anneal_frames: 5000
  target_allocation_movement_blend_anneal_frames: 5000
