# Frozen-policy structural probe: decoded latent token quality only refines
# sparse sensing-endpoint bids before capacity-two projection.
extends: exp_800_q4_u2u_hierarchical_multistatic_distributed_matching_hybrid50_semantic_decoder_eval.yaml

marl:
  comm_semantic_capacity_bid_enabled: true
  comm_semantic_capacity_bid_gain: 0.25
